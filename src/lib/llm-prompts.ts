export const mapInstallCommand =
  "npx shadcn@latest add unkn0wndfbx/mapcn-react-native/map";

export const mapInstallAgentPrompt = `Read the mapcn React Native agent instructions, then install mapcn in this React Native / Expo project that already uses NativeWind and React Native Reusables. Run ${mapInstallCommand}. Then add a basic example that imports Map and MapControls from "@/atoms/Map" and renders a map inside a sized container. Use the default tiled basemap for maps that need streets, labels, or geographic context. If the user wants a blank/data-only map, such as countries or country borders, use <Map blank> with MapGeoJSON and either a user-provided GeoJSON URL/object or a world countries GeoJSON source. Preserve the existing NativeWind and Reusables setup. Do not manually rewrite the registry component unless the command fails; if it fails, inspect the GitHub registry item with \`npx shadcn@latest view unkn0wndfbx/mapcn-react-native/map\` and install the listed dependencies.`;
